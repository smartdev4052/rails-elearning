<div class="container main mt-5">
	<div class="row">
		<div class="col-md-10 mt-3">
			<div class="row">
				<div class="col-md-12">
            		<h1><strong>Result: <%= @category.title %></strong></h1>
                        <h3 class="pt-3 text-muted"><i class="fa fa-user text-muted mb-1 mr-0"></i> 
                        <%= @lesson.user.name %> / <i class="fa fa-calendar text-muted mb-1 mr-0"></i>
                        <%= l @lesson.created_at, format: :long %></h3>
            	</div>
            </div>
        </div>
        <div class="col-md-2 center-block alert-danger pt-1 mb-1">
            <h2 class="text-center">Score<hr><%= @points %>/<%= @category.words.count %></h2>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <table class="table table-striped table-responsive mb-5">
                <thead>
                    <th></th>
                    <th >Words</th>
                    <th>Answers</th>
                    <th>Correct answers</th>
                </thead>
                <% @lesson_answers.each do |answer| %>
                <tbody>
                    <td class="answer_judge" style="width: 210px;">
                        <% if answer.choice.judge %>
                            <h3 class="text-center"><i class="fas fa-award"></i></h3>
                        <% else %>
                            <h3 class="text-center"><i class="fa fa-times text-danger"></i></h3>
                        <% end %>
                    </td>
                    <td style="width: 400px;">
                        <h3><%= answer.word.content %></h3>
                    </td>
                    <td style="width: 400px;">
                        <h3><%= answer.choice.content %></h3>
                    </td>
                    <td style="width: 400px;">
                        <h3><%= answer.word.correct_answer.content %></h3>
                    </td>
                </tbody>
                <% end %>
            </table>
            <div class="pagenate"><%= will_paginate @lesson_answers%></div>
        </div>
    </div>
</div>